import java.util.*;

class HouseRobber{
    int maxValue(ArrayList<Integer> houses,int currentIndex){
        int i;
        if(currentIndex > houses.size()){
            return 0;
        }
        else
        {
            for(i=houses.size()-1;i>=0;i--){
                int max_with_cur = houses.get();
        }
    }
    }
}